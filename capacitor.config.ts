import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ai.actyv.businesspartner.dev',
  appName: 'actyv for Business Partner Dev',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
