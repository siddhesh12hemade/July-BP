export enum AadhaarVerificationMethods {
  MANUAL_VERIFICATION = "MANUAL_VERIFICATION",
  E_VERIFICATION = "E_VERIFICATION",
}

export enum PanVerificationMethods {
  DEFAULT = "DEFAULT",
}

export enum GstVerificationMethods {
  DEFAULT = "DEFAULT",
}

export enum ItrVerificationMethods {
  DEFAULT = "DEFAULT",
}

export enum GstrVerificationMethods {
  DEFAULT = "DEFAULT",
}

export enum BankStatementVerificationMethods {
  UPLOAD = "UPLOAD",
  NET_BANKING = "NET_BANKING",
}

export enum UdyamVerificationMethods {
  DEFAULT = "DEFAULT",
}

export enum CINVerificationMethods {
  DEFAULT = "DEFAULT"
}

export enum GstrFilesVerificationMethods {
  DEFAULT = "DEFAULT",
}

export enum MiscellaneousDocumentVerificationMethods {
  DEFAULT = "DEFAULT",
}

export enum CustomFormVerificationMethods {
  DEFAULT = "DEFAULT"
}